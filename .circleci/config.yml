version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    working_directory: ~/app
    steps:
      - checkout
      - setup_remote_docker: # (2)
          docker_layer_caching: true # (3)
      - restore_cache: # special step to restore the dependency cache
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: Setup Dependencies
          command: npm install
      - save_cache: # special step to save the dependency cache
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules
      - run:
          name: "Deploy"
          command: |
            docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
            docker build --build-arg --quiet=false -t "hackdie/testing-docker:0.0.7" -f ./Dockerfile .
            docker tag "hackdie/testing-docker:0.0.7" "hackdie/testing-docker:latest"
            docker push hackdie/testing-docker:0.0.7
            docker push hackdie/testing-docker:latest

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
